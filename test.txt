a good instuction for hub
brew update
brew install hub
hub pull-request -m "message pull request" -b baseBranch -h Head
export prn=$(hub pull-request -m "message pull request" -b master -h kyosief-1)
#hub pr checkout PRNumber master 
hub pr checkout ${prn:lastvalue} master
#Make sure you rebase incase the local branch can not be merged using fast forward
Git rebase basebranch
git pull 
git checkout master 
git push 
git push origin master 


git config --global core.mergeoptions --no-edit

