brew update
brew install hub
hub pull-request -m "message pull request" -b master -h kyosief-1
hub pr checkout 5 master
git checkout master 
export prn=$(hub pr list)
echo var
echo $prn
git push 
git push origin master 

